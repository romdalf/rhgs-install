#-------------------------------------------------------------------------------------------#
# Ansible Playbook for RHGS
# 2019 - romuald@redhat.com 
#-------------------------------------------------------------------------------------------#
- hosts: "{{ host }}"
  gather_facts: false
  vars_prompt:
  - name: "rhn_user"
    prompt: "RHN Username"
    private: no
  - name: "rhn_pass"
    prompt: "RHN Password"
  - name: "rhn_pool"
    prompt: "RHN Pool"
    private: no
  roles:
  - role: roles/sysprep

- hosts: "{{ host }}"
  gather_facts: false
  roles: 
  - role: roles/rhgs_install

- hosts: "{{ host }}"
  gather_facts: false
  roles: 
  - role: roles/rhgs_nfs
    when: nfs=yes

- hosts: "{{ host }}"
  gather_facts: false
  roles: 
  - role: roles/rhgs_smb
    when: smb=yes

